# Resume-Analyzer-CSV-Generator
# AI-Powered Resume Analyzer

This application extracts structured information from resumes (PDF/DOCX)
using LangChain and LLMs and exports the results to CSV.

## Features
- Upload ZIP of resumes
- Automatic resume parsing
- Structured output with enforced schema
- CSV download
- Streamlit UI

## Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
